{
  "name": "sellkit",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/contracts",
    "packages/agent",
    "packages/web"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=packages/agent\" \"npm run dev --workspace=packages/web\"",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/contracts && npm run build --workspace=packages/agent && npm run build --workspace=packages/web",
    "build:vercel": "npm run build --workspace=packages/shared && npm run build --workspace=packages/web",
    "test": "npm run test --workspace=packages/contracts",
    "typecheck": "tsc -b packages/shared packages/agent packages/web"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "ts-node": "^10.9.2",
    "typescript": "^4.9.5"
  }
}
